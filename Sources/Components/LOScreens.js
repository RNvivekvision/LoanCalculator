import React from 'react';
import { StyleSheet, View } from 'react-native';
import { RNText } from '../Common';

const LOScreens = ({}) => {
  return (
    <View>
      <RNText>LOScreens</RNText>
    </View>
  );
};

const styles = StyleSheet.create({});

export default LOScreens;
